apiVersion: v1
kind: Service
metadata:
  name: {{ include "smym-keycloak.fullname" . }}
  labels:
    {{- include "smym-keycloak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smym-keycloak.selectorLabels" . | nindent 4 }}
